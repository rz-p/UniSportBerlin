export type Sport = {
    name: string,
    slug: string,
    location: string,
    schedule: string,
    details: string,
    image: string,
    participants: string[],
    participantCount: number,
}